// Define the function min that takes two arguments and returns their minimum.
function minimum(a, b) {
  return a < b ? a : b;
}

console.log(`The minimum number between 3 and 5 is ${minimum(3, 5)}`);
console.log(`The minimum number between 6 and 2 is ${minimum(6, 2)}`);
